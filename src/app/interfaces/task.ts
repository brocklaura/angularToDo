export interface Task {
    task:string,
    completed:boolean
}

export interface ngOnInit {
    ngOnInit(): void
}

export interface Todo {
    id: number,
    title: string,
    completed: boolean,
    editing: boolean
  }
  
 